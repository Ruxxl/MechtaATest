# Сверка покрытия: Mechta_ProductPage_TestCases.xlsx ↔ текущие автотесты

Источник: `/Users/ruslanisin/Downloads/Mechta_ProductPage_TestCases.xlsx` (100 кейсов,
листы `Test Cases` + `Сводка`). Изначально сверено 2026-08-04, обновлено 2026-08-04
после реализации блока 1 (breadcrumbs/gallery/gift/characteristics).

Действующее покрытие: `cypress/e2e/Regress_Test/product_page.cy.js` (старый, общий) +
`cypress/e2e/Regress_Test/product_page/product_onlyShopwindow/*.cy.js` (флаг "На
витрине") + `cypress/e2e/Regress_Test/product_page/{breadcrumbs_and_gallery,gift_modal,
characteristics_panel,cart_crosssell,favorites_compare,bonus_info,variant_selector}.cy.js`.

Статусы: **✅ Покрыт** / **🟡 Частично** / **❌ Не покрыт**.

## Блок 1. Основная информация (37 кейсов) — ЗАКРЫТ

| ID | Статус | Комментарий |
|---|---|---|
| TC-BC-01..02 | ✅ | `breadcrumbs_and_gallery.cy.js`, сверено с `categories` из API. Уточнено разведкой: товар в крошках ЕСТЬ (не ссылкой) — исходный файл здесь неточен |
| TC-BC-03 | ✅ | Негатив на несуществующий раздел — 404, не крашится |
| TC-GAL-01,03,04,05,07,10 | ✅ | `breadcrumbs_and_gallery.cy.js`, сверено с `images.length`. Уточнено разведкой: это горизонтальная лента всех изображений, а не подмена одного "главного" |
| TC-GAL-02,06,08,09,11 | ❌ | Товар без фото (нужна фикстура), вкладка "Видео" (у этого товара её нет), доп. случаи с модалкой |
| TC-INFO-01 | ✅ | `check_productName_copy_button` |
| TC-INFO-02 | ❌ | Скролл к отзывам по клику на рейтинг |
| TC-INFO-03,04,05,06,08 | ✅ | `gift_modal.cy.js`, сверено с `/offers.gifts` (вкладки, состав, добавление в корзину) |
| TC-INFO-07 | ❌ | Ошибка API при добавлении подарка (мок) |
| TC-INFO-09..12 | ❌ | "Комплектом дешевле" (bundles) — нужен товар с непустым `bundles` (не найден пока) |
| TC-INFO-13 | ✅ | `variant_selector.cy.js` — найдено пользователем как пропуск. Тот же механизм `/similar`, что и площадь помещения у кондиционеров, но с группами "Цвет корпуса" (type=image) и "Объём памяти" (type=text); на iPhone 17 Pro Max ещё и группа "Уценённые" (type=defective, `<a href>`, не `<button>`). Параметризовано на 3 товарах (8 тестов), после клика сверяются URL/isCurrent/название/цена/mainProperties |
| TC-INFO-14 | ❌ | Копирование артикула |
| TC-INFO-15..16 | ❌ | Ссылка на бренд |
| TC-INFO-17,18,19,23 | ✅ | `characteristics_panel.cy.js`, сверено с `/properties` (группы/поля, пустые группы корректно не рендерятся) и `finalPrice`. TC-INFO-19 уточнён разведкой: для анонимной сессии показывается модалка логина, а не прямой переход на чекаут (как и всюду в проекте) |
| TC-INFO-20 | ❌ | Ошибка добавления из панели (мок) |
| TC-INFO-21 | 🟡 | Проверка блока "Описание" на странице есть; вкладка "Описание" внутри панели характеристик не подтверждена разведкой отдельно |
| TC-INFO-22 | ❌ | Ошибка загрузки описания |

Побочно найдено и задокументировано: активный класс миниатюры галереи и кнопки
покупки в модалках/панели дублируют `id` с фоновой страницей (нужен явный скоуп через
`[role="dialog"]`); форс-клик по `pointer-events:none` оверлею стабильно валит
Electron-рендерер Cypress (см. skill п.4) — использовать `{esc}` вместо этого.

## Блок 2. Покупка и доставка (38 кейсов)

| ID | Статус | Комментарий |
|---|---|---|
| TC-CART-01..02 | ✅ | `cart_crosssell.cy.js`, сверено с `/relatedWithProviderData` (позитив живым товаром, негатив — замоканный пустой ответ, см. skill п.5.5) |
| TC-CART-03 | 🟡 | Тест написан и целенаправленно падает — см. **BUG-002** (ошибка API добавления в корзину показывает невидимую "успешную" панель вместо сообщения об ошибке) |
| TC-CART-04..08 | ✅ | `cart_crosssell.cy.js` — добавление сопутствующего товара (сверка через `/basket/`), граничный кейс `availability=notAvailable`, "Продолжить"/"В корзину" из панели |
| TC-CART-05 | 🟡 | Тот же **BUG-002** (общий эндпоинт добавления) — тест целенаправленно падает |
| TC-CART-09 | ✅ | `cart_crosssell.cy.js` — уточнено разведкой: анонимная сессия видит модалку логина, а не чекаут напрямую (как и TC-INFO-19) |
| TC-CART-10 | 🟡 | Буквально не воспроизводим для анонимной сессии (модалка логина показывается ДО любого запроса чекаута) — тест проверяет соседний инвариант (кнопка остаётся рабочей после сбоя) |
| TC-FAV-01..02 | ✅ | `favorites_compare.cy.js` — оптимистичная смена иконки + реальный `POST /api/v3/favorites/add`/`.../delete` с проверкой тела запроса и статуса. Персистентность через `cy.reload()` намеренно НЕ проверяется — `user_device_id` (анонимная идентичность) нестабилен первые секунды сессии, см. skill п.4 |
| TC-CMP-01..02 | ✅ | `favorites_compare.cy.js` — та же схема, `POST /api/v2/compare`/`.../delete` |
| BUG-003 (доп.) | 🟡 | `favorites_compare.cy.js` — ошибка API (500) при добавлении в избранное/сравнение роняет страницу необработанным исключением (unhandled promise rejection) вместо отката иконки. Тесты целенаправленно падают, документируют баг |
| TC-BONUS-01 | ✅ | `bonus_info.cy.js` — клик по строке "+N бонусов, +N фишек" (НЕ иконка "?", как в плане — такой на сайте нет), открывшаяся `[role="dialog"]` сверена с `bonuses`/`chips` из `/offers` |
| TC-BONUS-02..03 | 🟡 | Функциональность отсутствует на сайте — внутри модалки бонусов нет ссылки "Подробнее" (только кнопка закрытия), см. `BugReport/Товар/README.md` |
| TC-PREORDER-01 | ✅ | `preorder.cy.js` — живой пример найден через `/catalog/products?slug=smartfony&pageSize=100` постранично (Samsung Galaxy Z Flip8/Fold8, окно предзаказа активно до 17.08.2026). Уточнено разведкой: анонимная сессия видит модалку логина, а не переход на чекаут (как TC-CART-09/TC-INFO-19) |
| TC-PREORDER-02 | 🟡 | Буквально не воспроизводим для анонима (модалка логина ДО чекаут-запроса) — проверен соседний инвариант (кнопка не ломается после повторного клика), как TC-CART-10 |
| TC-STORE-01 | 🟡 | Переход к списку магазинов — покрыт косвенно (клик по `#product-shops` есть в `check_only_shop_adresses`), явного отдельного теста навигации нет |
| TC-STORE-02 | 🟡 | `store_navigation.cy.js` — буквально ("страница 404") не подтверждено разведкой для этой ошибки; проверен инвариант "страница не ломается" при 500 от `/subdivisions` |
| TC-STORE-03 | ✅ | `sticker_and_availability.cy.js` — поиск по фрагменту адреса |
| TC-STORE-04 | ✅ | `sticker_and_availability.cy.js` — несуществующий магазин |
| TC-STORE-05 | ✅ | Чекбокс "Скрыть витрину" + найден **BUG-001** на этом кейсе |
| TC-STORE-06 | ✅ | `store_navigation.cy.js` — сверено с `/subdivisions`: длинный список (11 магазинов) сворачивается + раскрывается кнопкой, короткий (5) виден сразу целиком без кнопки (граничный кейс) |
| TC-TRADEIN-01,03,04,06 | ✅ | `trade_in.cy.js` — фактически одна модалка на все четыре: строка "Trade-In" открывает диалог с видео-инструкцией (TC-TRADEIN-01/03 объединены — план описывает один экран с двух ракурсов), кнопка "Оценить устройство" открывает сторонний виджет Breezy (TC-TRADEIN-04), Esc закрывает (TC-TRADEIN-06) |
| TC-TRADEIN-02 | 🟡 | Smoke вместо буквального сценария ("404 со ссылкой на главную") — сторонний iframe не даёт контролируемо сымитировать именно такую ошибку; проверен инвариант "страница не ломается" при 500 от `/trade-in/instruction` |
| TC-TRADEIN-04 (доп.) | 🟡 | Тот же тест — см. **BUG-004**: клик "Оценить устройство" ИНОГДА (нестабильно, подтверждено и в Cypress, и в реальном Chrome) бросает необработанное исключение вместо открытия виджета |
| TC-TRADEIN-05 | ❌ | Не удалось надёжно сымитировать ошибку стороннего скрипта Breezy/видео — требует более глубокого исследования, как заблокировать именно нужный ресурс без падения остального |
| TC-TRADEIN-07 | ❌ | Явно не автоматизируется — сам автор плана пометил как "Нет" (полностью зависит от внешнего сервиса) |
| TC-BANNER-01..06 | 🟡 | Функциональность отсутствует на странице товара — баннер акции нигде не рендерится (0 картинок/ссылок/промо-классов в DOM), несмотря на непустой `promotions[]` в API, проверено на 2 товарах. Не баг, см. `BugReport/Товар/README.md` |

## Блок 3. Рейтинг и отзывы (8 кейсов)

| ID | Статус | Комментарий |
|---|---|---|
| TC-REV-01,02 | ✅ | `reviews.cy.js` — клик по вкладке (id `reka-tabs-...-trigger-{code}`) шлёт реальный `GET /reviews?sort={code}`, сверен ФАКТИЧЕСКИЙ порядок рейтингов в ответе (высокие/низкие действительно идут первыми, не только сам факт запроса) |
| TC-REV-03 | 🟡 | `reviews.cy.js` — запрос с `sort=helpful` и сохранение набора отзывов проверены; точный порядок НЕ проверяется — разведкой на 2 товарах подтверждено, что это не сортировка по `likes`/`dislikes`/id (значения не совпадают ни с одним видимым полем), формула бэка непрозрачна, кодировать предположение не стали |
| TC-REV-04 | 🟡 | Функциональность отсутствует на сайте — товар с `totalPages=2` не даёт способа увидеть отзывы сверх первых 6 (ни кнопки, ни пагинации). Не баг, см. `BugReport/Товар/README.md` |
| TC-REV-05,06 | ✅ | `reviews.cy.js` — `POST /api/v3/reviews/vote` переключает голос (`cancel:true/false`), сверены тело запроса, статус 204 и класс иконки до/после |
| TC-REV-07 | 🟡 | Тест написан и целенаправленно падает — см. **BUG-005** (анонимный лайк получает реальный 401 от API, но фронтенд никак не реагирует — ни модалки логина, ни отката, ни сообщения) |
| TC-REV-08 | ✅ | `reviews.cy.js` — при 500 иконка/счётчик не меняются вообще (нет ложного оптимистичного состояния, которое требовалось бы откатывать) |
| TC-REV-05,06 (доп., нестандарт) | ✅ | Явного TC в Excel нет — фото в отзывах проверены в рамках блока TC-REV. `photos[]` из API реально отрисовывается миниатюрами СТРОГО в карточке своего отзыва (`.closest('li')` от `#product-review-like-{index}`, количество миниатюр == `photos.length`); клик открывает `[role="dialog"]` с полноразмерным фото, счётчиком "N из M", навигацией и ПОЛНЫМ контекстом ИМЕННО этого отзыва (автор, звёзды рейтинга, текст, лайки/дизлайки); закрытие по Esc и по крестику (с учётом ~1.5с CSS-анимации) |

## Блок 4. Похожие товары (17 кейсов)

| ID | Статус | Комментарий |
|---|---|---|
| TC-SIM-01..10 | ❌ | Карусель "Похожие товары" — ВАЖНО: это ДРУГОЙ виджет, чем `/similar` (виджет выбора варианта по характеристике, уже покрыт в `page_blocks.cy.js`). "Похожие товары" внизу страницы показывает даже другие бренды — источник данных пока не подтверждён разведкой |
| TC-REL-01..07 | ❌ | Общая модалка сопутствующих товаров |

## Итог по всем 100 кейсам (блоки 2 и 3 ЗАКРЫТЫ)

- ✅ Покрыто полностью: **46** (блок 1: 20 + блок 2: 21 + блок 3: 5)
- 🟡 Частично: **19** (блок 1: 1 — TC-INFO-21; блок 2: 15; блок 3: 3 — TC-REV-03/04/07)
- ❌ Не покрыто: **35** (блок 1: 16; блок 2: 2 — TC-TRADEIN-05/07; блок 4: 17 целиком)

### Блок 3 (8 кейсов) — ЗАКРЫТ: 5 ✅ / 3 🟡 / 0 ❌

Реализован в `reviews.cy.js`. Найден **BUG-005** (Medium): анонимный лайк/дизлайк
получает от API корректный `401`, но фронтенд никак не реагирует — ни модалки логина
(как везде в проекте для похожих действий — корзина, чекаут, предзаказ, избранное),
ни отката, ни сообщения об ошибке (TC-REV-07 намеренно падает). TC-REV-04 ("Показать
все отзывы") — функциональность из плана отсутствует на сайте, не баг. Для TC-REV-03
("Полезные") сознательно не стали утверждать конкретный порядок сортировки — живой
проверкой на 2 товарах подтверждено, что это не `likes`/`dislikes`/id, реальная
формула бэка не видна через API и кодировать догадку означало бы нарушить методологию
"верифицируй, не предполагай". Дополнительно (сверх исходных 8 кейсов, явного TC в
Excel нет) проверены фото в отзывах: `photos[]` приходит с бэка и рендерится
в карточке своего отзыва, галерея при открытии показывает данные ИМЕННО этого
отзыва (рейтинг, лайки/дизлайки, текст), закрывается по Esc и по крестику.

### Блок 2 (38 кейсов) — ЗАКРЫТ: 21 ✅ / 15 🟡 / 2 ❌

Все 10 файлов-областей блока 2 реализованы: `cart_crosssell`, `favorites_compare`,
`bonus_info`, `preorder`, `store_navigation`, `trade_in` (плюс переиспользованные
`sticker_and_availability` для TC-STORE-03/04/05 и `variant_selector` для TC-INFO-13
из блока 1, найденного и покрытого попутно).

**Найдено и задокументировано 3 новых бага** (сверх BUG-001 из блока 1) — все по
одному и тому же классу дефекта: обработчики клика на странице товара систематически
не оборачивают запрос/ответ в try/catch:
- **BUG-002** (High): ошибка API добавления в корзину показывает невидимую панель
  "успеха" вместо сообщения об ошибке (TC-CART-03/05 намеренно падают).
- **BUG-003** (Medium): ошибка API добавления в избранное/сравнение роняет страницу
  необработанным исключением (TC-FAV/TC-CMP негативные тесты намеренно падают).
- **BUG-004** (Medium, нестабильно воспроизводится): клик "Оценить устройство" в
  Trade-In иногда бросает необработанное исключение вместо открытия виджета —
  подтверждено и в Cypress, и напрямую в реальном Chrome.

**🟡 Частично (15) — по причине:**
- Намеренно падающие тесты, документирующие баги: TC-CART-03/05 (BUG-002),
  TC-TRADEIN-04-доп (BUG-004).
- Функциональность из плана отсутствует на сайте (не баг): TC-BONUS-02/03 (нет
  ссылки "Подробнее"), TC-BANNER-01..06 (баннер не рендерится вообще).
- Сценарий не воспроизводим буквально для анонимной сессии (модалка логина
  показывается ДО чекаут-запроса, проверен соседний инвариант): TC-CART-10,
  TC-PREORDER-02.
- Smoke вместо буквального сценария ошибки (сторонний/недоступный роут не даётся
  контролируемо сымитировать): TC-STORE-02, TC-TRADEIN-02.
- Покрыт косвенно, без отдельного целевого теста: TC-STORE-01.

**❌ Не покрыто (2):** TC-TRADEIN-05 (не удалось надёжно сымитировать ошибку
стороннего скрипта), TC-TRADEIN-07 (сам автор плана пометил как не автоматизируемое).

Побочно за время работы над блоком 2 поймано и исправлено несколько своих же багов в
тестах — стоит держать в голове на будущее (см. skill п.4): `cy.contains(selector,
text).filter(...)` возвращает только ПЕРВОЕ совпадение, из-за чего `.filter()`
после него может стабильно получать 0 элементов, если на странице несколько похожих
кандидатов — правильно `cy.get(selector).filter(...)`; опечатка в одну букву
("Объём"/"Объем") может выглядеть неотличимо от гонки состояний и потратить много
времени на "починку" несуществующей проблемы, если не проверить название буквально
через API в первую очередь.

Блоки 3 (Рейтинг и отзывы, 8 кейсов) и 4 (Похожие товары, 17 кейсов) остаются
полностью непокрытыми — следующий кандидат, если продолжать в том же порядке.

## План закрытия (по приоритету из файла)

Порядок реализации — по `Приоритет` из исходного файла (High → Medium → Low), новые
файлы под `cypress/e2e/Regress_Test/product_page/`, по одному на блок функционала
(`gallery.cy.js`, `gift_and_bundle.cy.js`, `characteristics_panel.cy.js`,
`cart_crosssell.cy.js`, `favorites_compare.cy.js`, `reviews_interactions.cy.js`,
`similar_related.cy.js`, `misc_blocks.cy.js` для баннера/бонусов/trade-in/предзаказа).
Каждый новый блок — сначала живая разведка (см. skill п.1), затем тесты. Кейсы с
пометкой "Частично"/"Нет" в столбце "Автоматизация" исходного файла (мок недоступного
роута, hover, 3rd-party Trade-In виджет, тайминги баннера) реализуются в объёме,
подтверждённом комментарием автора файла — как smoke-проверка, а не 1:1.
